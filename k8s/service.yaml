apiVersion: v1
kind: Service
metadata:
  name: david-vizena-service
  namespace: david-vizena
  labels:
    app: david-vizena-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: david-vizena-app
